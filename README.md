# Text Frequency Analyzer


A python script that analyzes a text file and identifies the most frequent words.
You can run it directly from the command line.

## Input
- **Text File** – Path to the `.txt` file you want to analyze.

## Output
- Prints the most frequent words along with their counts.
